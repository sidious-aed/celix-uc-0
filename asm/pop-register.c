#include "/home/tyrel/celix/standard.h"

quadrant main(quadrant naof_params, source_vecter params) {
	asm("pop %r8");
	asm("pop %r9");
	asm("pop %r10");
	asm("pop %r11");
	asm("pop %r12");
	asm("pop %r13");
	asm("pop %r14");
	asm("pop %r15");
	asm("pop %rax");
	asm("pop %rbx");
	asm("pop %rcx");
	asm("pop %rdx");
	asm("pop %rdi");
	asm("pop %rsi");
	asm("pop %rsp");
	asm("pop %rbp");
	return 0;
}
