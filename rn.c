#include "./standard.h"

quadrant main(quadrant naof_params, source_vecter ** params) {
  if(naof_params != 2) {
    printf("params | 1\n");
    printf("1 | node\n");
    return 0;
  }
  syscall(unix_rmdir, params[1], archive_arws);
  return 0;
}
